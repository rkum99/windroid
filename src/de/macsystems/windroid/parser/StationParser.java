package de.macsystems.windroid.parser;

/**
 * 
 */

/**
 * @author Jens Hohl
 * @version $Id$
 * 
 */
public class StationParser
{

	/**
	 * @param args
	 */
	public static void main(final String[] args) throws Exception
	{

		// WindUtils.updateStationList();
		//
		// final SAXParserFactory factory = SAXParserFactory.newInstance();
		// final SAXParser parser = factory.newSAXParser();
		// parser.parse(IOUtils.getCachedStationURL().openStream(), new
		// StationHandler());
		//
		// for (final Continent each : Continent.values())
		// {
		// System.out.println("" + each.getID());
		// final Iterator<Country> countrys = each.iterator();
		// while (countrys.hasNext())
		// {
		// final Country country = countrys.next();
		// System.out.println(">" + country.getName());
		// final Iterator<Region> regions = country.iterator();
		// while (regions.hasNext())
		// {
		// final Region region = regions.next();
		// System.out.println(">>" + region.getName());
		// System.out.println(">>" + region.getID());
		// final Iterator<StationBean> stations = region.iterator();
		// while (stations.hasNext())
		// {
		// final StationBean station = stations.next();
		// System.out.println(">>>> " + station.toString());
		// }
		// }
		//
		// }
		// }

		// final URL url = WindUtils.getStationXMLUrl();
		// final SAXParserFactory factory = SAXParserFactory.newInstance();
		// final SAXParser parser = factory.newSAXParser();
		// parser.parse(url.openStream(), new StationHandler());

	}
}
